torch==2.13.0
botorch==0.18.1
moocore==0.3.2
scipy==1.18.0
numpy==2.5.1
matplotlib==3.11.0
pandas==3.0.4
gpytorch==1.15.2
scikit-learn==1.9.0
loguru==0.7.3
# Optional: image-based context embeddings (contextual optimization, README 8.13).
# Install manually to keep the default setup slim:
#   python -m pip install open_clip_torch pillow
#
# Optional: MetaTAF backend (multi-objective Meta-BO, README 8.14). Needs the openbo
# fork with the multi-objective optimizers; install manually (requires git):
#   python -m pip install "open-bo @ git+https://github.com/M-Colley/openbo@main"
# To refresh an existing install (openbo stays at version 0.1.0, so pip's --upgrade
# considers it already satisfied; the backend refuses installs predating the 2026-08
# TAF-R rework):
#   python -m pip install --force-reinstall --no-deps "open-bo @ git+https://github.com/M-Colley/openbo@main"
# Not auto-installed here on purpose: machines without git (e.g. the bundled Python
# setup) would otherwise fail this whole requirements install. The backend prints this
# exact command if openbo is missing. openbo's dependency floors are satisfied by the
# pinned versions above (gpytorch sits exactly on openbo's minimum - keep in sync).
